#include "main.h"
#include <stdlib.h>
/**
 * _abs - Function definition
 *
 * Description: A program that computes the absolute value of an integer
 * @n: parameter for accept character
 *
 * Return: Nothing
 */
int _abs(int n)
{
	return (abs(n));
}
