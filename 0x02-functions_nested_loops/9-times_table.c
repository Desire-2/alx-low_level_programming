#include <stdio.h>
#include "main.h"

#define SIZE 10
/**
 * times_table -  Prints the 9 times table,
 *  starting with 0
 *
 *  Return: Always 0
 */
void times_table(void)
{
int i,j;

int mat[SIZE][SIZE];

mat[i][j] = (i * j);

if (i = 0; i < SIZE; i++);
 (j = 0; j < SIZE; j++);

            
{
printf("%4d", mat[i][j]);

printf("\n")
}
return (o)
}
